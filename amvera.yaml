meta:
  environment: nodejs
  toolchain:
    name: npm
    version: 22

build:
  command: npm install --cache /tmp/.npm-cache && npm run build

run:
  command: ./node_modules/.bin/vite preview --port 80 --host
