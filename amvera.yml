meta:
  environment: nodejs
  toolchain:
    name: npm
    version: 20

build:
  # Устанавливаем зависимости
  command: npm install --cache /tmp/.npm-cache && npm run build

run:
  # Запускаем ваш сервер
  command: node server.js
